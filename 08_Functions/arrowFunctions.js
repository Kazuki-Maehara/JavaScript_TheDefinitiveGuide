// Make a copy of an array with null elemtns removed.
let filtered = [1, null, 2, 3, ].filter(x => x !== null);
console.log(filtered);

// Square some numbers.
let squares = [1, 2, 3, 4, ].map(x => x * x);
console.log(squares);
